const initialState = {
  // States related to user
  userInfo: {}, // { email: , password:, ..., totalSavings: }
}

const iHearth = (state=initialState, action) => {
  switch (action.type){
    case '':
      return {

      }
  }
}